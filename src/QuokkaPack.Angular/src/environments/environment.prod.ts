// Production environment configuration
export const environment = {
  production: true,
  // Set this to your production API URL
  // Example: 'https://api.quokkapack.com' or 'https://quokkapack.com/api'
  apiBaseUrl: '' // Same origin as frontend in production
};
