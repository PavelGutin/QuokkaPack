import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TripPack } from './trip-pack';

describe('TripPack', () => {
  let component: TripPack;
  let fixture: ComponentFixture<TripPack>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [TripPack]
    })
    .compileComponents();

    fixture = TestBed.createComponent(TripPack);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
